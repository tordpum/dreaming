#include <stdio.h>
#include <stdlib.h>

long long int main()
{
    int i,a[300];
    long long int m,n,sum;
    scanf("%lld",&n);
    scanf("%lld",&m);
    printf("%lld",n+m);
    return 0;
}
